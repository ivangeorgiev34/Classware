@using Classware.Areas.Administrator.Models.Teacher;
@model ICollection<AllTeachersViewModel>

<form method="post" asp-area="Administrator" asp-controller="Teacher" asp-action="Delete">
	<table class="table table-striped table-bordered">
		<thead>
			<tr>
				<th class="text-center" scope="col">Profile picture</th>
				<th class="text-center" scope="col">First name</th>
				<th class="text-center" scope="col">Middle name</th>
				<th class="text-center" scope="col">Last name</th>
				<th class="text-center" scope="col">Age</th>
				<th class="text-center" scope="col">Gender</th>
				<th class="text-center" scope="col">Username</th>
				<th class="text-center" scope="col">Subject</th>
				<th class="text-center" scope="col">Actions</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var teacher in Model)
			{
				<tr>
					@if (@teacher.ProfilePicture == null)
					{
						<td class="text-center">No profile picture</td>
					}
					else
					{
						<td class="text-center"><img src="data:image;base64,@teacher.ProfilePicture" style="border-radius:50%;height:150px; width:150px;" /></td>
					}
					<td class="text-center">@teacher.FirstName</td>
					<td class="text-center">@teacher.MiddleName</td>
					<td class="text-center">@teacher.LastName</td>
					<td class="text-center">@teacher.Age</td>
					<td class="text-center">@teacher.Gender</td>
					<td class="text-center">@teacher.Username</td>
					@if (@teacher.SubjectName == null)
					{
						<td class="text-center">No subject</td>
					}
					else
					{
						<td class="text-center">@teacher.SubjectName</td>
					}
					<td class="text-center"><button class="btn btn-danger" asp-area="Administrator" asp-controller="Teacher" asp-action="Delete" asp-route-id="@teacher.Id">Delete</button></td>
				</tr>
			}
		</tbody>
	</table>
</form>
